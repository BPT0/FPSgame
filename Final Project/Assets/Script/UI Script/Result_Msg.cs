using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Result_Msg : MonoBehaviour
{
    /*private bool isEnabled = false;

    // 필요한 오브젝트
    [SerializeField] private GameObject End_Msg;
    [SerializeField] private Text isEnd;
    [SerializeField] private Text R_txt;
    [SerializeField] private Text S_txt;
    [SerializeField] private Text A_txt;
    private StartMsg theStartMsg;

    private void Start()
    {
        isEnd.text = "Playing";
        R_txt.text = "Rock R X" + theStartMsg.getRock().ToString();
        S_txt.text = "Rock S X" + theStartMsg.getSapaier().ToString();
        A_txt.text = "Rock A X" + theStartMsg.getAmethyst().ToString();
    }

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.G))
            Show_Result();
    }

    
    public void Show_Result()
    {
        isEnabled = !isEnabled;
        End_Msg.SetActive(isEnabled);
        R_txt.text = "Rock R X " + theStartMsg.R_count.ToString();
        S_txt.text = "Rock S X " + theStartMsg.S_count.ToString();
        A_txt.text = "Rock A X " + theStartMsg.acou.ToString();
    }*/
}
